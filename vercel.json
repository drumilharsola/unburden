{
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git /vercel/.flutter-sdk && cd unburden_app && /vercel/.flutter-sdk/bin/flutter pub get",
  "buildCommand": "cd unburden_app && /vercel/.flutter-sdk/bin/flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL",
  "outputDirectory": "unburden_app/build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
